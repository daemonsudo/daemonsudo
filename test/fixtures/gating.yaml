# auto/deny stage fixture; the approve path has no approver in tests, so it
# parks and times out quickly.
defaults: approve
timeout: 1500ms
rules:
  read_*: auto
  drop_*: deny
redact:
  - "*.password"
channels:
  web:
    port: 14910
